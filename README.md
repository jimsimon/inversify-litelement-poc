# inversify-litelement-poc
This repo contains a proof of concept for connecting Inversify to components
built with LitElement (or any custom element framework).

Run it via ```npm start```

Build it via ```npm build```